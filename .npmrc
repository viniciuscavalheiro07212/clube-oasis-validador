# Expo/React Native frequentemente têm conflitos de peerDependencies entre
# react / react-native / pacotes da comunidade. legacy-peer-deps mantém o
# comportamento de resolução compatível (necessário p/ expo install --fix).
legacy-peer-deps=true
